package scanner;

/** A runtime exception thrown when no transition is found when sending a
 * character in a finale state automata. */
public class NoTransitionFoundException extends RuntimeException
{
}

