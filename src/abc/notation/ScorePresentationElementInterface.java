package abc.notation;

/** A tagging interface for all elements composing the score of a tune that are
 * releated to score presentation for rendition. */
public interface ScorePresentationElementInterface extends ScoreElementInterface
{
}

